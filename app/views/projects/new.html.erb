<% content_for :title, "New Project" %>

<%= simple_form_for @project do |f| %>
  <%= f.input :title, error_prefix: "Title" %>
  <%= f.input :description, error_prefix: "Description" %>
  <%= f.submit %>
<% end %>